import 'package:flutter/material.dart';

class MessageSwap extends StatelessWidget {
  const MessageSwap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Messagem Swap");
  }
}